# Архитектура LLM Datacenter

Обзор для работы над любой задачей. Детали конкретной области — в `docs/features/*`. Метод — `CONVENTIONS.md`.

## Слои
<!-- Опиши поток от входа до БД. Пример:
UI/страницы  →  actions/контроллеры (валидация + guard)  →  сервисы (бизнес-логика + транзакции)  →  доступ к данным
Параллельно: чистые функции (общие клиенту/серверу), схемы валидации, клиентские сторы.
-->

## Инварианты (нельзя ломать)
<!-- Пронумерованный список правил, которые НЕ нарушаются. Примеры:
1. ...
2. ...
-->

## Матрица доступа
<!-- Кто что видит/может по состояниям сущностей. Таблица. -->

## Модели данных
<!-- Ключевые сущности и связи (ссылка на схему БД). -->

## Сквозные паттерны
<!-- Как делается мутация, оптимистичные обновления, фильтры-в-URL, санитизация и т.п. -->

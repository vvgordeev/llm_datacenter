# Roadmap

Что планируется, но ещё не оформлено в `docs/requirements/`. Оформленное — переезжает в инкремент и удаляется отсюда.

- Подключить трекер задач (Linear) и обновить п.7 в AGENTS.md.
- Управление источниками из UI дашборда (сейчас — правка sources.yaml в репо).
- Прекомпиляция месячных срезов, если загрузка ~30 дневных JSON станет заметной.
- Уведомления о сводке (email/Telegram) после ежедневного прогона.
